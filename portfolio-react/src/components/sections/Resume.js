import React from "react";

const Resume = () => {
    return (
        <section className="resume-section">
            this will be a popover of my resume
        </section>
    );
}

export default Resume;